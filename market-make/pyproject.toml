[project]
name = "market-make"
version = "0.1.0"
description = "Stock market data visualization with Laravel and Python"
requires-python = ">=3.11"
dependencies = [
    "yfinance>=0.2.0",
    "pandas>=2.0",
    "numpy>=1.24",
]

